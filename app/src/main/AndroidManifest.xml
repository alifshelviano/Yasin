<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/kakungicon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/kakungicon"
        android:supportsRtl="true"
        android:theme="@style/Theme.Yasin"
        tools:targetApi="31">

        <activity
            android:name="com.kemat.yasin.PrepareActivity"
            android:exported="false"
            android:theme="@style/Theme.Yasin"/>
        <activity
            android:name="com.kemat.yasin.MenuActivity"
            android:theme="@style/Theme.Yasin"
            android:exported="false" />
        <activity
            android:name="com.kemat.yasin.Tampil"
            android:theme="@style/Theme.Yasin"
            android:exported="false" />
        <activity
            android:name="com.kemat.yasin.ActivityDoa"
            android:theme="@style/Theme.Yasin"
            android:exported="false" />

        <activity android:name="com.kemat.yasin.MainActivity" />

        <activity

            android:name="com.kemat.yasin.SplashScreen"

            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>